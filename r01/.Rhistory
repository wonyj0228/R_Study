name <- '홍길동'
name <- 'hong'
name
age <- 100
age
install.packages("ggplot2")
#install.packages("ggplot2")
install.packages("corrplot") #상관도를 그려주는 라이브러리
library(corrplot)
install.packages('lattice')
library(lattice)
library(corrplot)
library(lattice)
a=mtcars
a
mcor2 = cor(mtcars$gear, mtcars$carb)
mcor2
xplot(gear~carb, data=mtcars)
xyplot(gear~carb, data=mtcars)
lm = plot(mtcars$gear, mtcars$carb)
abline(lm((mtcars$gear, mtcars$carb)))
abline(lm((mtcars$gear~mtcars$carb)))
mcor = cor(mtcars)
mcor
mcor
round(mcor,2)
# 상관계수를 도표로
corrplot(mcor)
# 여러 도표
plot(mtcars)
abline(lm((mtcars$gear~mtcars$carb)))
abline(lm((mtcars$gear~mtcars$carb)))
abline(lm((mtcars$gear~mtcars$carb)))
mcor
abline(lm((mtcars$gear~mtcars$carb)))
# 산점도와 회귀선을 그려줌
lm = plot(mtcars$gear, mtcars$carb)
abline(lm((mtcars$gear~mtcars$carb)))
# 전체 상관 분석의 소수점이 너무 길어서 소수점 2째 자리까지 정리
round(mcor,2)
# 여러 도표
plot(mtcars)
# 상관계수를 도표로
corrplot(mcor)
# wr와 mpg와의 상관 관계
cor(mtcars$wt, mtcars$mpg)
qplot(wt, mpg, data=mtcars, color=factor(crab))
cor(mtcars$wt, mtcars$mpg)qplot(wt, mpg, data=mtcars, color=factor(crab))
# wr와 mpg와의 상관 관계
cor(mtcars$wt, mtcars$mpg)
qplot(wt, mpg, data=mtcars, color=factor(crab))
gplot(wt, mpg, data=mtcars, color=factor(crab))
qplot(wt, mpg, data=mtcars, color=factor(crab))
# wr와 mpg와의 상관 관계
library(ggplot2)
cor(mtcars$wt, mtcars$mpg)
qplot(wt, mpg, data=mtcars, color=factor(crab))
qplot(wt, mpg, data=mtcars, color=factor(carb))
mcor
# 전체 상관 분석의 소수점이 너무 길어서 소수점 2째 자리까지 정리
round(mcor,2)
library(ggplot2)
install.packages('ggmap')
library(ggmap)
# R에서는 c함수 라는 것을 사용해서 데이터를 묶어준다
seoul <- c(left=126.77, bottom=37.40, right-127.17, top=37.70)
# R에서는 c함수 라는 것을 사용해서 데이터를 묶어준다
seoul <- c(left=126.77, bottom=37.40, right=127.17, top=37.70)
map <- get_stamenmap(seoul, zoom=12, maptype = "terrain")
ggmap(map)
######## ass find modify! #########
pop <- read.csv(file.choose(), header = T)
pop
pop
